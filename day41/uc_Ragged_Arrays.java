package com.days.day41;

public class uc_Ragged_Arrays {
    public static void main(String[] args) {

        //Each row in a two-dimensional array is itself an array.Thus, the rows can have different lengths.An array of this
        //kind is known as a ragged array.
        //Usually when we  create an array we need to declare to row and column numbers but this time we don't have to mention on the first lane about column number.
        //ornek sayfa 15. int[][] triangleArray=new int[5][];
    }
}
